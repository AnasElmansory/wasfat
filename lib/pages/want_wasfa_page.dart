import 'package:flutter/material.dart';

class WantWasfa extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('اريد وصفه'),
        centerTitle: true,
      ),
      body: Container(),
    );
  }
}
